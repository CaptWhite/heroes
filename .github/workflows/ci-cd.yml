name: Despliegue en VPS con Docker Compose

on:
  push:
    # Ejecuta el workflow cada vez que se haga un push a la rama 'main'
    branches:
      - main
  workflow_dispatch: # Permite ejecución manual desde GitHub UI

jobs:
  deploy:
    runs-on: ubuntu-latest # Ejecuta en un runner hospedado por GitHub

    steps:
      - name: ⬇️ Checkout del código
        uses: actions/checkout@v4
        
      # --- Configuración SSH para Conexión al VPS ---
      - name: 🔑 Configurar SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          # Clave privada SSH de tu usuario en el VPS.
          # Debe estar almacenada en Secrets de GitHub (ej: SSH_PRIVATE_KEY)
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      # --- Despliegue Remoto en el VPS ---
      # Paso 4: Desplegar
# Job de Despliegue (Job 'deploy')
# ...

# En tu archivo ci-cd.yml
# ...

      # Paso 4: Desplegar
      - name: 🚀 Deploy to VPSs via SSH
        env:
          HOST: ${{ secrets.SSH_HOST }}
          USER: ${{ secrets.SSH_USER }}
        run: |
          # 1. Definimos la CADENA DE COMANDOS REMOTOS en una variable local
          # Usamos comillas simples (') para proteger los comandos de Docker
          # de cualquier expansión local.
          REMOTE_COMMANDS='
            cd /home/$USER/workspaces/heroes &&
            docker compose pull &&
            docker compose down &&
            docker compose --file docker-compose-prod.yml up -d &&
            echo "Despliegue de contenedores finalizado y levantado."
          '
          # 2. Ejecutamos SSH de forma explícita
          # Usamos la sintaxis: ssh user@host "comandos"
          ssh -o StrictHostKeyChecking=no "${USER}@${HOST}" "${REMOTE_COMMANDS}"
          